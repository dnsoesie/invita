import classes from "./Header.module.css";

const Header = () => {
    return (
        <header className={classes.header}>
            <div>
                <p>U.S. Cities Reviews</p>
            </div>
        </header>
    )
}

export default Header;